export enum ERRORS {
    UNKNOWN_ROUTES = 'API not found...',
    INTERNAL = 'Internal Error.'
}

export enum LEAGUES_ERROR_ENUM {
    LEAGUE_NOT_FOUND = 'League not found.',
    LEAGUES_NOT_FOUND = 'Leagues not found.',
}

export enum TEAMS_ERROR_ENUM {
    TEAM_PLAYERS_NOT_FOUND = 'The Team has no players.',
    TEAMS_NOT_FOUND = 'Teams not found.',
}